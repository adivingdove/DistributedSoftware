<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.distributed.inventory.mapper.SeckillOrderMapper">

    <resultMap id="BaseResultMap" type="com.distributed.inventory.entity.SeckillOrder">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="product_id" property="productId" jdbcType="BIGINT"/>
        <result column="product_name" property="productName" jdbcType="VARCHAR"/>
        <result column="price" property="price" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, product_id, product_name, price, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.distributed.inventory.entity.SeckillOrder">
        INSERT INTO seckill_order (id, user_id, product_id, product_name, price, status, create_time, update_time)
        VALUES (#{id}, #{userId}, #{productId}, #{productName}, #{price}, #{status}, NOW(), NOW())
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndProductId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <update id="deductStock">
        UPDATE product SET stock = stock - 1, update_time = NOW()
        WHERE id = #{productId} AND stock > 0
    </update>

</mapper>
